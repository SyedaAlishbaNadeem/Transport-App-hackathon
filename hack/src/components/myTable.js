import React from 'react'

export default function Table() {
  return (
    <div>
      

      {/* <Grid container sx={{ padding: 10, margin: 2, border: '1px solid black'}}>
        <Box>
          <Table item md={4}>
            <TableHead>
              <TableCell> </TableCell>
              <TableCell> </TableCell>
              <TableCell></TableCell>
              <TableCell></TableCell>
              <TableCell></TableCell>
            </TableHead>

              {list.map((x, i) => {
                return (
                  <TableBody>
                    <TableCell key={i}> {x.abc} </TableCell>
                    <TableCell> {x} </TableCell>
                    <TableCell> {x} </TableCell>
                    <TableCell> {x} </TableCell>
                    <TableCell> {x} </TableCell>
                  </TableBody>
                );
              })}
         
          </Table>
        </Box>
      </Grid> */}













    </div>
  )
}
